{
  "name": "@sinsan/rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-native Korean Janggi rules engine (immutable position, legal move generation, game result detection).",
  "license": "AGPL-3.0-or-later",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test '../../tests/rules/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
