{
  "name": "sinsan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test:rules": "node --test 'tests/rules/**/*.test.ts'",
    "test:model": "node --test 'tests/model/**/*.test.ts'",
    "test": "node --test 'tests/rules/**/*.test.ts' 'tests/model/**/*.test.ts'"
  }
}
